This is a development version following the 0.3.0 release.

Package size is above 5 MB because the package includes documentation, vignettes, and small test fixtures for exercising OSRM graph preparation and server-management workflows. OSRM backend binaries are not bundled in the package; they are downloaded on demand by `osrm_install()`.
