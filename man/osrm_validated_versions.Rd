% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/osrm_validated_versions.R
\name{osrm_validated_versions}
\alias{osrm_validated_versions}
\title{Fetch validated OSRM versions from per-OS badge JSON}
\usage{
osrm_validated_versions(
  os = c("linux", "macos", "windows", "all"),
  repo = "e-kotov/osrm.backend",
  branch = "badges",
  expand = TRUE,
  gh_token = NULL
)
}
\arguments{
\item{os}{Character, one of \code{"linux"}, \code{"macos"}, \code{"windows"}, or \code{"all"}.
\code{"linux"} reads the \code{osrm_versions_ubuntu.json} badge. \code{"all"} returns a
named list with entries for each OS.}

\item{repo}{Character, repository hosting the badges. Defaults to
\code{"e-kotov/osrm.backend"}.}

\item{branch}{Character, the branch where badges are stored. Defaults to
\code{"badges"}.}

\item{expand}{Logical, if \code{TRUE} and the badge message encodes a simple
range (e.g. \code{v5.27.1 - v26.7.3}), attempt to expand the range to the list
of available release tags from \code{e-kotov/osrm-binaries}. If expansion
fails, the function falls back to returning the raw badge message.}

\item{gh_token}{Optional GitHub token for API requests. By default the
function does not use authentication (public raw.githubusercontent URLs).}
}
\value{
If \code{os != "all"}: a list with elements \code{label}, \code{message}, \code{raw}
(the parsed JSON). If \code{expand = TRUE} and expansion succeeded, returns a
character vector of tag names (newest first). If \code{os = "all"}, returns a
named list with one entry per OS.
}
\description{
The package's live integration workflow publishes per-OS JSON "badges"
(for example \code{osrm_versions_ubuntu.json}) to the \code{badges} branch. These
files contain a short message describing the validated range or latest
validated versions. This helper fetches that JSON and (optionally) expands
a range to the list of release tags available in the \code{e-kotov/osrm-binaries}
repository.
}
\examples{
\dontrun{
osrm_validated_versions("linux")
osrm_validated_versions("all", expand = FALSE)
}
}
